export {
  app,
  auth,
  db,
  firebaseConfig,
  microsoftProvider,
  RecaptchaVerifier,
  signInWithPhoneNumber,
  storage,
} from "@/lib/firebase/firebaseClient";

export { default } from "@/lib/firebase/firebaseClient";